<%= form_tag poll_comment_path(Comment.find(id).poll, Comment.find(id)), id: "edit-comment-form-#{id}", class: 'comment-edit-form', method: :put, remote: true do %>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
<% end %>