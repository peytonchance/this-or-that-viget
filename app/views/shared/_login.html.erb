<dialog hidden class="form log-in">
  <form class="form__inner">
    <div class="form__top">
      <h2 class="form__title">Log in</h2>
      <button href="/" class="form__cancel" data-module="modal" data-modal="log-in">Cancel<i class="icon-expired"></i></button>
    </div>
    <div class="form__fields">
      <%= form_for(:user, url: registration_path(:user)) do |f| %>
        <div class="form__fields__field">
         <%= f.label :email %>
         <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
        </div>

        <div class="form__fields__field">
          <%= f.label :password %>
          <%= f.password_field :password, autocomplete: "off" %>
        </div>

        <div class="form__fields__actions log-in__submit">
          <%= f.submit "Log in", class: "form__button" %>
          <button>Forgot Password?</button>
        </div>
      <% end %>
    </div>
  </form>
  <div class="form__bottom-prompt">
    <p>New here?</p>
    <button data-module="modal" data-modal="sign-up">Sign up</button>
  </div>
</dialog>
